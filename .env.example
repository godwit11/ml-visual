# Nya 对话接口的配置样例
#
# 用法：
#   本地开发 —— 复制这个文件为 `.env.local`，填上真实值。
#               `.env.local` 已在 .gitignore 里，不会被提交。
#   线上部署 —— 把同名变量填到 Vercel 项目的 Environment Variables 里。
#
# ⚠️ 千万不要把真实密钥写进这个文件（`.env.example` 是要进仓库的）。

# 必填：MiniMax 的 API Key
MINIMAX_API_KEY=

# 选填：接口地址。MiniMax 有**两套账号体系**，地址不同：
#   国内账号  https://api.minimax.cn/v1
#   国际账号  https://api.minimax.io/v1
# 不填则默认走国内。填错会返回 401 / 404 —— 换另一个试试即可。
# 判断方法：看你在哪个站点买的套餐（platform.minimaxi.com 是国内）。
MINIMAX_BASE_URL=https://api.minimax.cn/v1

# 选填：模型名。不填默认 MiniMax-M3。
# 可选值（截至 2026-09）：MiniMax-M3 / MiniMax-M2.7 / MiniMax-M2.7-highspeed /
#                        MiniMax-M2.5 / MiniMax-M2.5-highspeed / MiniMax-M2.1 / MiniMax-M2
# 如果你的套餐不包含默认模型，会返回"模型不存在"之类的错误，改成你有权限的即可。
MINIMAX_MODEL=MiniMax-M3

# 选填：允许调用这个接口的来源，逗号分隔。
# 其他来源的请求会被拒绝 —— 这能挡住别的网站盗用你的代理。
# 不填则使用内置默认值（含 www.godwit.asia 和本地开发地址）。
NYA_ALLOWED_ORIGINS=

# --------------------------------------------------------------------------
# 「报告问题」浮层用的邮件通道（Resend）
#
# 不填也能跑 —— 服务端会退化成「把报告全文写进服务端日志」
# （Vercel 的 Runtime Logs 里能看到），并在页面上如实告诉用户
# 「通道还没开通」。它**不会假装发送成功**。
# --------------------------------------------------------------------------

# 必填：Resend 的 API Key（在 resend.com 注册后，去 API Keys 页面创建）
RESEND_API_KEY=

# 必填：报告发到哪个邮箱（你自己的收件地址）
REPORT_TO=

# 选填：发件人。默认值 onboarding@resend.dev 是 Resend 给**未验证域名**账号的
# 测试发件地址 —— 用它只能发到账号自己的注册邮箱，但对「反馈发给自己」
# 这个场景刚好够用，所以**不验证域名也能立刻用起来**。
# 验证了 godwit.asia 之后可以换成：ML 演示站 <feedback@godwit.asia>
# （那时也能发给任意地址，且更容易过垃圾邮件过滤）。
REPORT_FROM=onboarding@resend.dev

# --------------------------------------------------------------------------
# 「报告问题」浮层用的邮件通道（Resend）
#
# 不填也能跑 —— 服务端会退化成「把报告全文写进服务端日志」
# （Vercel 的 Runtime Logs 里能看到），并在页面上如实告诉用户
# 「通道还没开通」。它**不会假装发送成功**。
# --------------------------------------------------------------------------

# 必填：Resend 的 API Key（在 resend.com 注册后，去 API Keys 页面创建）
RESEND_API_KEY=

# 必填：报告发到哪个邮箱（你自己的收件地址）
REPORT_TO=

# 选填：发件人。默认值 onboarding@resend.dev 是 Resend 给**未验证域名**账号的
# 测试发件地址 —— 用它只能发到账号自己的注册邮箱，但对「反馈发给自己」
# 这个场景刚好够用，所以**不验证域名也能立刻用起来**。
# 验证了 godwit.asia 之后可以换成：ML 演示站 <feedback@godwit.asia>
# （那时也能发给任意地址，且更容易过垃圾邮件过滤）。
REPORT_FROM=onboarding@resend.dev

